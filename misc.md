`➜  boostcom-dev git:(master) brew upgrade terraform # update specific brew package`

`git branch --merged | egrep -v "(^\*|master|dev)" | xargs git branch -d
` - remove all branches which has been merged

